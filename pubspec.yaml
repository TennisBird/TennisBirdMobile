name: tennis_bird_mobile
description: "Tennis bird mobile"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  dio:
  flutter_secure_storage:
  get_it:
  internet_connection_checker_plus: ^2.5.2
  easy_localization: ^3.0.7
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  rive: 
  core: 
    path: core/
  splash: 
    path: features/splash/
  welcome:
    path: features/welcome/
  navigation: 
    path: navigation/
  data:
    path: data/
  domain: 
    path: domain/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  flutter_gen: 
  melos: ^6.1.0

flutter:
  uses-material-design: true
