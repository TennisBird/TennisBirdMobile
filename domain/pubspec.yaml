name: domain
description: "domain_class"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  core: 
    path: ../core
  data:
    path: ../data
  flutter_secure_storage: ^9.2.2
  dartz:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_gen:  
  build_runner: 

flutter_gen:
  output: lib/util/