name: core
description: "Core libraries holder"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  dartz: ^0.10.1
  internet_connection_checker_plus:
  dio:  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

